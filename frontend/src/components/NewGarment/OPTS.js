const LIMBS = ['arm'];

const COMPRESSCLASS = ['class 1', 'class 2', 'class 3', 'class 4'];

const TYPES = [
  'sleeve without gauntlet',
  'sleeve with gauntlet',
  'gauntlet',
  'glove',
];

const MPOINTS = ['a', 'b', 'c', 'd', 'e', 'f', 'g'];

export { LIMBS, TYPES, COMPRESSCLASS, MPOINTS };
